services:
  grok2api:
    build: .
    container_name: grok2api
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - /root/grok2api/data:/app/data
      - /root/grok2api/logs:/app/logs